## 1. 输出字母N形图案

本题目要求打印字母N形状的图形。输入是一个整数n，表示图形有n行。例如输入5，输出

```text
*   *
**  *
* * *
*  **
*   *
```

```c
#include <stdio.h>
int main()
{
    int i,j;
    int n;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        for(j=0;j<n; j++)
            if(【__________（5分）】)
                【__________（3分）】;
            else putchar(' ');
        【__________（2分）】;
    }
    return 0;
}
```

---

## 2. 找出最大公因子和最小公倍数

本题目要求计算两个正整数的最大公因子和最小公倍数。

```c
#include <stdio.h>
int main()
{int  m,n,r,s;
  scanf("%d,%d",&m,&n);
  s=m*n;
  if(m<n)
    
  【__________（2分）】

  do
      { r=【__________（2分）】;
         m=【__________（2分）】;
         n=【__________（2分）】;
      }while(n);
   printf("%d %d\n",m,【__________（2分）】);
  return 0;
}
```

---

## 3. 哪个同学哪门课的成绩最高1

班上有7个同学，学4门课程。题目要求找出哪个同学哪门课成绩最高。
### 输入样例

```text
95 99 95 88
89 74 95 100
95 47 87 50
86 78 90 76
51 99 86 71
43 64 73 56
80 86 81 94
```


### 输出样例

```text
student 1 course 3 is excellent
```

```c
#include <stdio.h>
int main()
{ float score[7][4],max;
  int maxhang,maxlie,i,j;
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        scanf("%d",【__________（3分）】);
  
        【__________（3分）】

  max=score[0][0];
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        if( max<score[i][j])
             
        【__________（4分）】

  printf("student %d course %d is excellent",maxhang, maxlie);
  return 0;
}
```

---

## 4. 哪个学生哪门课最高2

班上有7个同学，学4门课程。题目要求找出哪个同学哪门课成绩最高。
### 输入样例

```text
95 99 95 88
89 74 95 100
95 47 87 50
86 78 90 76
51 99 86 71
43 64 73 56
80 86 81 94
```


### 输出样例
```text
student 1 course 3 is excellent
```

```c
#include <stdio.h>
void find(float score[7][4],int *hang,int *lie)
{ int i,j;
  float max;
  
  【__________（3分）】

  max=【__________（3分）】;
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        if( max<score[i][j])
             {max=score[i][j];【__________（3分）】}
}

int main()
{ float score[7][4],max;
  int maxhang,maxlie,i,j,k;
  for(k=0; k<3; k++) {
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        scanf("%d",【__________（2分）】);
  find(【__________（4分）】);
  printf("student %d course %d is  excellent\n",  maxhang, maxlie);
 }
  return 0;
}
```

---

## 5. 哪个学生哪门课成绩最高3

班上有7个同学，学4门课程。题目要求找出哪个同学哪门课成绩最高。
### 输入样例

```text
95 99 95 88
89 74 95 100
95 47 87 50
86 78 90 76
51 99 86 71
43 64 73 56
80 86 81 94
```


### 输出样例
```text
student 1 course 3 is excellent
```

```c
#include <stdio.h>
void find(float *score,int *hang,int *lie)
{ int i,j;
  float max;
  *hang=0,*lie=0;
  max=【__________（3分）】;
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        if(【__________（3分）】)
             {
             【__________（3分）】;  *hang=i;   *lie=j;  }
}

int main()
{ float score[7][4],max;
  int maxhang,maxlie,i,j;
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        scanf("%d",&score[i][j]);
  find(【__________（3分）】);
  printf("student %d course %d is  excellent\n",  maxhang, maxlie);
  return 0;
}
```

---

## 6. 哪个同学哪门课成绩最高4

班上有7个同学，学4门课程。题目要求找出哪个同学哪门课成绩最高。
### 输入样例

```text
95 99 95 88
89 74 95 100
95 47 87 50
86 78 90 76
51 99 86 71
43 64 73 56
80 86 81 94
```


### 输出样例
```text
student 1 course 3 is excellent
```

```c
#include <stdio.h>
void find(float (*p)[4], int *hang, int *lie)
{ int i,j;
  float max;
   *hang=0;*lie=0;
   max=【__________（3分）】;
   for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        if(【__________（3分）】)
            {【__________（3分）】;
              *hang=i;
              *lie=j;
           }
}

int main()
{ float score[7][4],max;
  int maxhang,maxlie,i,j;
  for (i=0; i<7;i++)
     for(j=0; j<4; j++)
        scanf("%d",&score[i][j]);
  find(【__________（3分）】);
  printf("student %d course %d is excellent\n",  maxhang, maxlie);
  return 0;
}
```

---

## 7. 串操作练习

本题目要求完成字符串的比较、复制、连接操作。
### 输入用例

```text
showed
show
```


### 输出用例
```text
0
```

```c
#include <stdio.h>
int strcompare(char *s,char *t)  //串的比较,返回0或不同处ASCII码的差
{  while(【__________（2分）】)
      s++,t++;
   return 【__________（2分）】;
}
void strcopy(char *s,char *t)   //串复制，功能与strcpy相同
{
   while(【__________（2分）】)
   { 
   【__________（2分）】; s++; t++;  }
【__________（2分）】;
}
void strconcat(char *s,char *t)  //串的连接，功能与stcat相同
{
    while(【__________（2分）】) 
        s++;
    while(【__________（2分）】)
    {
    【__________（2分）】;
        s++; t++;
    }
    【__________（2分）】;
}
int main()
{ char a[80],b[80],c[80];
  int i;
  gets(a);gets(b);
  strcopy(c,b);
  strconcat(c,"ed");
  printf("%d\n",strcompare(a,c));
  return 0;
}
```

---

## 8. 单词长度

读入一行文本，其中以空格分隔为若干个单词，以‘.’结束。你要输出这行文本中每个单词的长度。这里的单词与语言无关，可以包括各种符号，比如“it's”算一个单词，长度为4。注意，行中可能出现连续的空格。
### 输入格式:
输入在一行中给出一行文本，以‘.’结束，结尾的句号不能计算在最后一个单词的长度内。

### 输出格式：
在一行中输出这行文本对应的单词的长度，每个长度之后都有空格。

### 输入样例：
```text
It's great to see you here.
```

### 输出样例：
```text
4 5 2 3 3 4
```

```c
#include <stdio.h>
#include<string.h>
int main( )
{ int i, k, word,len;
  char string[81];
  gets(string);
  len=strlen(string);
  word=0; k=0;
  for(i=0;【__________（5分）】; i++)
    if (string[i]== ' '||string[i]== '.')
    { if(word==1) printf("%d ",k);
        
    【__________（5分）】

    }
    else  { 
    【__________（5分）】
      }
  }
  return 0;
```

---

## 9. 字符串排序-填1

输入10个字符串，本题目要求排序后输出这些串（从小到大）。

```c
#include <stdio.h>
#include<string.h>
int main( )
{ char string[10][20],t[20];
  int i,j,k;
  for(i=0; i<10; i++)
     gets(【__________（2分）】);
  for(i=0; i<9; i++)
    for(j=【__________（2分）】; j<10; j++)
        if(【__________（5分）】)
            {   
            【__________（5分）】

            }
  for(i=0; i<10; i++)
    puts(【__________（1分）】);
  }
  return 0;
}
```

---

## 10. 字符串排序-填2

输入10个字符串，本题目要求利用函数对字符串排序后，输出这些串（从小到大）。

```c
#include <stdio.h>
#include<string.h>
void sort(char s[ ][20])
{ char t[20];
  int  i,j,k;
  for(i=0; i<9; i++)
    for(j=i+1; j<10; j++)
        if(【__________（5分）】)
            {   
            【__________（5分）】

            }
}
int main( )
{ char string[10][20],t[20];
  int i,j,k;
  for(i=0; i<10; i++)
     gets(string[i]);
  sort(【__________（5分）】);
  for(i=0; i<10; i++)
    puts(string[i]);
  }
  return 0;
```

---

## 11. 字符串排序-填3

输入10个字符串，本题目要求利用元素指针对字符串排序后，输出这些串（从小到大）。

```c
#include <stdio.h>
#include<string.h>
void sort(char *s)
{ char t[20];
  int  i,j,k;
  for(i=0; i<9; i++)
    for(j=i+1; j<10; j++)
        if(【__________（5分）】)
            {   
            【__________（5分）】

            }
}
int main( )
{ char string[10][20],t[20];
  int i,j,k;
  for(i=0; i<10; i++)
     gets(string[i]);
  sort(【__________（5分）】);
  for(i=0; i<10; i++)
    puts(string[i]);
  return 0;
}
```

---

## 12. 字符串排序-填4

输入10个字符串，本题目要求利用行指针对字符串排序后，输出这些串（从小到大）。

```c
#include <stdio.h>
#include<string.h>
void sort(char (*s)[20])
{ char t[20];
  int  i,j,k;
  for(i=0; i<9; i++)
    for(j=i+1; j<10; j++)
        if(【__________（5分）】)
            {   
            【__________（5分）】

            }
}
int main( )
{ char string[10][20],t[20];
  int i,j,k;
  for(i=0; i<10; i++)
     gets(string[i]);
  sort(【__________（5分）】);
  for(i=0; i<10; i++)
    puts(string[i]);
  return 0;
}
```

---

## 13. 字符串排序-填5

输入10个字符串，本题目要求利用指针数组或指向指针的指针对字符串排序后，输出这些串（从小到大）。

```c
#include <stdio.h>
#include<string.h>
void sort(【__________（5分）】)
{ char t[20],*w;
  int  i,j,k;
  for(i=0; i<9; i++)
    for(j=i+1; j<10; j++)
        if(【__________（5分）】)
            {   
            【__________（5分）】

            }
}
int main( )
{ char string[10][20];
  char *p[10];
  int s,i,j,k;
  for(s=0;s<3; s++){
  for(i=0; i<10; i++)
  【__________（5分）】;
  for(i=0; i<10; i++)
     gets(p[i]);
  sort(【__________（5分）】);
  for(i=0; i<10; i++)
    puts(p[i]);
  }
  return 0;
}
```

---

## 14. 整数排序-填1

本题目要求利用函数对12个整数从小到大排序。

```c
#include <stdio.h>
#include<string.h>
void sort(int *s)
{ int w,t;
  int  i,j,k;
  for(i=0; i<11; i++)
    for(j=0; j<11-i; j++)
        if(【__________（2分）】)
            {  
            【__________（2分）】

            }
}
int main( )
{ int  a[12];
  int i,j,k;
  for(i=0; i<12; i++)
     scanf("%d",【__________（2分）】);
  sort(【__________（2分）】);
  for(i=0; i<12; i++)
    printf("%3d",【__________（2分）】);
  return 0;
}
```

---

## 15. 整数排序-填2

本题目要求利用指针数组或指向指针的指针对12个整数进行从小到大的排序。

```c
#include <stdio.h>
#include<string.h>
void sort(【__________（3分）】)
{ int t,*w;
  int  i,j,k;
  for(i=0; i<11; i++)
    for(j=0; j<11-i; j++)
        if(【__________（3分）】)
            {  
            【__________（3分）】

            }
}
int main( )
{ int  a[12];
  int *p[12];
  int i,j,k;
  for(i=0; i<12; i++)
  【__________（3分）】;
  for(i=0; i<12; i++)
     scanf("%d",【__________（3分）】);
  sort(【__________（3分）】);
  for(i=0; i<12; i++)
    printf("%3d",【__________（3分）】);
  return 0;
}
```

