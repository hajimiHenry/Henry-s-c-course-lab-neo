#include <stdio.h>

int mian(void)
{

    int N;
    scanf("%d",&N);
    
    int p,q;
    int count[N][q];

    for (int i = 0; i < N; i++)
    {
        scanf("%d",&p);
        for (int j = 0; j < p; j++)
       {
        scanf("%d",&count[i][j]);
        /* code */
       }
        /* code */
    }
    

}